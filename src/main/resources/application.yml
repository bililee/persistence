# 端口的配置
server:
  port: 8081
  # 数据库访问配置
spring:
  type : com.alibaba.druid.pool.DruidDataSource
  datasource:
    driver-class-name : com.mysql.cj.jdbc.Driver
  #将database_name改成自己的数据库的名称
    url: jdbc:mysql://localhost:3306/database_name?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 12345678